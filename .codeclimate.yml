version: "2"
checks:
  similar-code:
    config:
      threshold: 120
exclude_patterns:
    - 'tests/'
    - 'spec/'
    - '**/*.spec.tsx'
    - '**/*.spec.ts'
    - '**/*.spec.js'
    - '**/vendor/'
    - '**/node_modules/'
